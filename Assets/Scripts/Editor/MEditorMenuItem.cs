using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MEditorMenuItem : MonoBehaviour
{
    private MEditorMenuItem parentAlias;
    private string keyAlias;

    void Init(MEditor)

    // Update is called once per frame
    void Update()
    {
        
    }
}
